services:
  playwright-tests:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: playwright-tests
    ipc: host
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CI=true
    command: npx playwright test --project=chromium
